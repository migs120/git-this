<h1>Items new</h1>
<%= form_for [ @scategory, @item ] do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>
 
   <%= f.label :body %>
   <%= f.text_area :body %>
 
   <%= f.submit "Save" %>
 <% end %>
